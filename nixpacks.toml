# Configuración para Railway/Nixpacks (monorepo backend)
[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm install"]

[phases.build]
cmds = ["npm run frontend:build", "npm run backend:build"]

[start]
cmd = "cd backend && node dist/main.js"
